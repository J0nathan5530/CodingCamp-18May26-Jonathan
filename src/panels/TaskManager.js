/**
 * TaskManager — full CRUD to-do list with localStorage persistence.
 *
 * Implemented in Task 7.1.
 */

export function initTaskManager(rootEl) {
  // TODO: implement in Task 7.1
  rootEl.innerHTML = `<h2>Tasks</h2><p>No tasks yet.</p>`;
}
